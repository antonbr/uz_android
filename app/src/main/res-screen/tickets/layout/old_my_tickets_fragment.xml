<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/tools"
                xmlns:card_view="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginBottom="@dimen/bottomBarHeight"
                android:orientation="vertical">

    <include layout="@layout/toolbar_back"/>

    <LinearLayout
        android:id="@+id/ticketDateLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_marginLeft="@dimen/tickets_padding"
        android:layout_marginRight="@dimen/tickets_padding"
        android:layout_marginTop="@dimen/tickets_padding"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <View
            android:layout_width="45dp"
            android:layout_height="1dp"
            android:background="@color/textColorHint"/>

        <TextView
            android:id="@+id/ticketDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="sans-serif-medium"
            android:gravity="center_horizontal"
            android:textAllCaps="true"
            android:textColor="@color/accentColor"
            android:textSize="@dimen/button_text_size"/>

        <View
            android:layout_width="45dp"
            android:layout_height="1dp"
            android:background="@color/textColorHint"/>
    </LinearLayout>

    <android.support.v4.view.ViewPager
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/ticketDateLayout"
        android:paddingBottom="@dimen/tickets_pagger_padding_bottom"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:overScrollMode="never"/>

    <com.viewpagerindicator.IconPageIndicator
        android:id="@+id/pageIndicator"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tickets_bullet_size"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/tickets_padding"/>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>
</RelativeLayout>